// scriptHome.js

$(document).ready(function () {
    // Optional: Add fade-in or entrance animations
    $('#mainTitle').hide().fadeIn(800);
    $('.subtitle').hide().delay(300).fadeIn(800);
    $('.intro-card').hide().delay(600).fadeIn(800);
  });
